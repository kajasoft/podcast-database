option: max_matches=20000, field_weights=(item_title=10,feed_title=3,item_keywords=3,item_categories=3)
facets:
- explicit:
    title: Explicit
    order: desc
    values: 
    - 1 Explicit
- tag_ids:
    limit: 500
    select: special
    order: "count(*) desc"
    before: true
    after: false
sorts:
- . | Relevance 
